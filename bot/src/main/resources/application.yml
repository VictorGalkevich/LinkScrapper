app:
  telegram-token: ${BOT_TOKEN}

spring:
  application:
    name: bot
  jackson:
    time-zone: UTC
  devtools:
    restart:
      enabled: false

server:
  port: 8090

logging:
  config: classpath:log4j2-plain.xml

